<dl>
  <%= link_to 'home', root_path %>
  <% if user_signed_in? %>
    welcome <%= current_user.email %>
    <%= '(ADMIN)' if current_user.admin? %>,
    <%= link_to 'sign out', destroy_user_session_path, :method => :delete %>
  <% else %>
    <%= link_to 'log in', new_user_session_path %>
    <%= link_to 'sign up', new_user_registration_path %>
  <% end %>
  
  <dt>Mappings:</dt>
  <dd><%= link_to 'new', new_mapping_path %> <%= link_to 'list', mappings_path %></dd>
  <dt>Bad Phrases:</dt>
  <dd><%= link_to 'new', new_bad_phrase_path %> <%= link_to 'list', bad_phrases_path %></dd>
  <dt>Good Phrases:</dt>
  <dd><%= link_to 'list', good_phrases_path %></dd>
  <dt><%= link_to 'suggestions', polite_suggestions_path %></dd>
</dl>